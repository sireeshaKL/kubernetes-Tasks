version: "2"
services:
  app:
    build: ./app
    links:
      - db
    ports:
      - 5001:5000
    volumes:
      - /root/mnt/mntvolume:/var/lib/mysql
  db:
    image: mysql:5.7
    environment:
       MYSQL_ROOT_PASSWORD: root
    container_name: compose-container1
    volumes:
      - ./db:/docker-entrypoint-initdb.d:ro
